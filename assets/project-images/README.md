Add project thumbnail images here (PNG/JPG/WebP).

Files currently referenced by `index.html`:

- `bim254-crispr.png`
- `mat226c-stokesian.png`
- `ech256-active-matter.png`

Quick way to create thumbnails:
1. Open your slide deck.
2. Export one representative slide as PNG.
3. Save with the names above (or update names in `index.html`).
